# agenterrafrom

Empty Terraform scaffold for AWS provider.

## Quick start

1. Copy `terraform.tfvars.example` to `terraform.tfvars` and adjust values.
2. Run `terraform init`.
3. Run `terraform plan`.
